import React, { useState, useEffect } from 'react';
import PropTypes from 'prop-types';

const IPInput = (props) => {
  return (
    <div>
    </div>
  );
};

IPInput.propTypes = {
};

export default IPInput;
